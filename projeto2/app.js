const calculos = require('./calculos')

var soma = calculos.soma;

console.log(soma(4,5));

var multi = calculos.multi

console.log(multi(2,3));

var sub = calculo.sub;
console.log(sub(5,5));
